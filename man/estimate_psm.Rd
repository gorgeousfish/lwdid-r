% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_psm.R
\name{estimate_psm}
\alias{estimate_psm}
\title{Estimate PSM-ATT (Propensity Score Matching)}
\usage{
estimate_psm(
  data,
  y,
  d,
  propensity_controls,
  n_neighbors = 1L,
  with_replacement = TRUE,
  caliper = NULL,
  caliper_scale = "sd",
  trim_threshold = 0.01,
  se_method = "abadie_imbens",
  n_bootstrap = 200L,
  seed = NULL,
  alpha = 0.05,
  match_order = "data"
)
}
\arguments{
\item{data}{data.frame, cross-sectional data (one row per unit)}

\item{y}{character, outcome variable name}

\item{d}{character, treatment indicator name (1=treated, 0=control)}

\item{propensity_controls}{character vector, PS model covariates}

\item{n_neighbors}{integer, number of nearest neighbors (default 1)}

\item{with_replacement}{logical, match with replacement (default TRUE)}

\item{caliper}{numeric or NULL, caliper width}

\item{caliper_scale}{character, "sd" or "absolute"}

\item{trim_threshold}{numeric, PS trimming threshold (default 0.01)}

\item{se_method}{character, "abadie_imbens", "reuse_adjusted", or "bootstrap"}

\item{n_bootstrap}{integer, bootstrap replications (default 200)}

\item{seed}{integer or NULL, random seed}

\item{alpha}{numeric, significance level (default 0.05)}

\item{match_order}{character, matching order}
}
\value{
list with 16 fields
}
\description{
Estimate PSM-ATT (Propensity Score Matching)
}
