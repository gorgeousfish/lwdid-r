% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/staggered.R
\name{estimate_staggered_effects}
\alias{estimate_staggered_effects}
\title{Estimate staggered (g,r) treatment effects}
\usage{
estimate_staggered_effects(
  dt,
  y,
  ivar,
  tvar,
  gvar,
  rolling,
  control_group,
  controls,
  vce,
  cluster_var,
  alpha,
  pre_stats,
  min_obs = 3L,
  min_treated = 1L,
  min_control = 1L,
  estimator = "ra",
  ps_controls = NULL,
  trim_threshold = 0.01,
  trim_method = "clip",
  n_neighbors = 1L,
  caliper = NULL,
  caliper_scale = "sd",
  with_replacement = TRUE,
  match_order = "data",
  se_method = NULL,
  boot_reps = 200L,
  seed = NULL,
  return_diagnostics = FALSE,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{dt}{data.table, complete panel data}

\item{y}{character, outcome variable name}

\item{ivar}{character, unit identifier variable name}

\item{tvar}{character, time variable name}

\item{gvar}{character, cohort variable name}

\item{rolling}{character, transformation method
(\code{"demean"} or \code{"detrend"})}

\item{control_group}{character, resolved control group strategy
(not \code{"auto"})}

\item{controls}{character vector or NULL, control variable names}

\item{vce}{character or NULL, VCE method}

\item{cluster_var}{character or NULL, cluster variable name}

\item{alpha}{numeric, significance level (default 0.05)}

\item{pre_stats}{named list from \code{precompute_transforms()}}

\item{min_obs}{integer, minimum total observations (default 3L)}

\item{min_treated}{integer, minimum treated units (default 1L)}

\item{min_control}{integer, minimum control units (default 1L)}
}
\value{
data.frame with 17 columns, one row per (g,r) effect
}
\description{
Estimates ATT for each valid (cohort, period) pair in
staggered DiD designs. Implements lw2025 Procedure 4.1 and
lw2026 equation 7.8.

The estimation proceeds in 8 steps for each (g,r) pair:
\enumerate{
\item Extract period r cross-section (period_data)
\item Select control group on period_data (get_valid_controls)
\item Build treatment mask and estimation sample (sub)
\item Construct treatment indicator (d_sub) + min sample checks
\item Apply precomputed transform (apply_precomputed_transform)
\item Extract controls + two-step NA filter + min_obs check
\item Run RA estimator (estimate_ra_common)
\item Store results (17-column data.frame row)
}

Corresponds to Python \code{estimate_cohort_time_effects()}.
}
\keyword{internal}
