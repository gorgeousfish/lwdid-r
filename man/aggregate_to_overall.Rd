% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{aggregate_to_overall}
\alias{aggregate_to_overall}
\title{Overall weighted effect estimation}
\usage{
aggregate_to_overall(
  dt,
  y,
  ivar,
  tvar,
  gvar,
  cohorts,
  T_max,
  pre_stats,
  rolling,
  vce = NULL,
  cluster_var = NULL,
  alpha = 0.05,
  controls = NULL
)
}
\arguments{
\item{dt}{data.table, complete panel data}

\item{y}{character, outcome variable name}

\item{ivar}{character, unit identifier variable name}

\item{tvar}{character, time variable name}

\item{gvar}{character, cohort variable name}

\item{cohorts}{integer vector, all cohorts to estimate}

\item{T_max}{integer, maximum time period}

\item{pre_stats}{named list, precomputed transform statistics}

\item{rolling}{character, transform method ("demean" or "detrend")}

\item{vce}{character or NULL, VCE type ("homoskedastic", "HC1", "cluster")}

\item{cluster_var}{character or NULL, cluster variable name
(required when vce="cluster")}

\item{alpha}{numeric, significance level (default 0.05)}

\item{controls}{character vector or NULL, control variable names
(optional extension beyond paper eq 7.19)}
}
\value{
A named list containing the overall ATT estimate (\code{att}), its
standard error (\code{se}), confidence interval bounds (\code{ci_lower},
\code{ci_upper}), test statistics (\code{t_stat}, \code{pvalue}), original and effective
cohort weights (\code{cohort_weights}, \code{effective_weights}), sample-size and
degrees-of-freedom diagnostics (\code{n_treated}, \code{n_control}, \code{n_total},
\code{df_resid}, \code{df_inference}, \code{K}), controls metadata (\code{controls_tier},
\code{controls_used}), and the aggregated-outcome diagnostics returned by
\code{construct_aggregated_outcome()}.
}
\description{
Implements lw2026 equations 7.18-7.19 to estimate the overall
weighted ATT tau_omega via OLS regression. The workflow computes cohort
weights omega_g = N_g / N_treat (eq. 7.12), constructs the aggregated
outcome with \code{construct_aggregated_outcome()} (eq. 7.18, including
FATAL-002 protection), runs \code{estimate_ra_common()} on \code{Y_bar ~ D_ever}
with optional controls (eq. 7.19), and extracts tau_omega as the
coefficient on \code{D_ever}.

This function corresponds to Python \code{aggregate_to_overall()}, but the R
implementation keeps both \code{cohort_weights} and \code{effective_weights}, uses an
explicit three-tier controls degradation path through
\code{estimate_ra_common()}, and stores \code{df_resid}, \code{df_inference}, \code{n_total},
\code{K}, \code{controls_tier}, \code{controls_used}, and \code{diagnostics} as explicit result
fields.
}
\keyword{internal}
