% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity.R
\name{estimate_propensity_score}
\alias{estimate_propensity_score}
\title{Estimate propensity scores via logistic regression}
\usage{
estimate_propensity_score(
  data,
  d,
  controls,
  trim_threshold = 0.01,
  trim_method = c("clip", "drop")
)
}
\arguments{
\item{data}{data.frame or data.table containing treatment indicator and controls}

\item{d}{character, treatment indicator variable name (1=treated, 0=control)}

\item{controls}{character vector, control variable names (length >= 1)}

\item{trim_threshold}{numeric, trimming threshold in (0, 0.5), default 0.01}

\item{trim_method}{character, "clip" (default) or "drop"}
}
\value{
list with 8 elements:
\describe{
\item{propensity_scores}{numeric vector (clip: clipped PS; drop: PS with NA)}
\item{coefficients}{named list with "_intercept" key}
\item{converged}{logical, whether glm converged}
\item{n_trimmed}{integer, number of trimmed observations}
\item{trimmed_mask}{logical vector (meaningful only for drop mode)}
\item{trim_method}{character, actual trim method used}
\item{glm_object}{glm object for downstream vcov() calls}
\item{removed_cols}{character vector of removed constant columns}
}
}
\description{
Uses unpenalized logistic regression (MLE via IRLS) to estimate
P(D=1|X). Returns propensity scores, coefficients, and the glm object
for downstream IF-based SE computation.
}
\references{
lw2025 Procedure 3.1 Step 2;
MLE for \eqn{P(D=1 \mid X)} under a Bernoulli log-likelihood.
}
\keyword{internal}
