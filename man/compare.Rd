% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare}
\alias{compare}
\title{比较多个 lwdid 估计结果}
\usage{
compare(
  ...,
  type = c("overall", "effects"),
  stats = c("estimate", "std.error", "ci", "p.value"),
  stars = TRUE,
  digits = 3L
)
}
\arguments{
\item{...}{命名的 lwdid_result 对象，或一个包含它们的列表}

\item{type}{字符型。比较类型："overall"（默认，整体 ATT）或
"effects"（分期效应）}

\item{stats}{字符向量。包含的统计量。默认：
c("estimate", "std.error", "ci", "p.value")}

\item{stars}{逻辑值。是否添加显著性星号？默认 TRUE}

\item{digits}{整数。小数位数。默认 3}
}
\value{
一个 \code{lwdid_comparison} S3 对象（继承 data.frame），包含：
\itemize{
\item 每个模型 specification 为一列
\item 统计量（估计值、标准误、CI、p 值等）为行
\item 格式化的 print 方法
}
}
\description{
比较不同 specification（估计器、转换方法、控制组策略）下的 lwdid 估计结果，
以标准化表格形式输出。基于 Lee & Wooldridge (2025, 2026) 的 ATT 定义。

不同 rolling 方法给出的是同一目标参数 tau 在不同识别假设下的估计，
compare() 的数学意义在于展示 ATT 在不同假设和方法下的稳健性。
}
\examples{
\donttest{
  res1 <- structure(list(
    att = 0.5, se_att = 0.1, t_stat = 5.0, pvalue = 0.001,
    ci_lower = 0.3, ci_upper = 0.7, nobs = 100L,
    n_treated = 50L, n_control = 50L, rolling = "demean",
    estimator = "ra", vce_type = "HC3", method = "proc21"
  ), class = "lwdid_result")

  res2 <- structure(list(
    att = 0.6, se_att = 0.15, t_stat = 4.0, pvalue = 0.005,
    ci_lower = 0.31, ci_upper = 0.89, nobs = 100L,
    n_treated = 50L, n_control = 50L, rolling = "demean",
    estimator = "ipw", vce_type = "HC3", method = "proc21"
  ), class = "lwdid_result")

  # Compare two specifications
  compare(RA = res1, IPW = res2)

  # Compare with effects breakdown
  compare(RA = res1, IPW = res2, type = "effects")
}
}
\seealso{
\code{\link{tidy.lwdid_result}}, \code{\link{print.lwdid_comparison}}

Other lwdid-results: 
\code{\link{extract_effects}()},
\code{\link{glance.lwdid_result}()},
\code{\link{lwdid_result}},
\code{\link{new_lwdid_result}()},
\code{\link{print.lwdid_comparison}()},
\code{\link{tidy.lwdid_result}()},
\code{\link{tidy_glance}}
}
\concept{lwdid-results}
