% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/randomization.R
\name{ri_staggered}
\alias{ri_staggered}
\title{Staggered Randomization Inference}
\usage{
ri_staggered(
  data,
  y,
  ivar,
  tvar,
  gvar,
  observed_att,
  target = c("overall", "cohort", "cohort_time"),
  target_cohort = NULL,
  target_period = NULL,
  rolling = "demean",
  controls = NULL,
  vce = NULL,
  cluster_var = NULL,
  reps = 1000L,
  seed = NULL,
  method = c("permutation", "bootstrap"),
  parallel = FALSE
)
}
\arguments{
\item{data}{data.table, original panel data.}

\item{y}{Character, outcome variable name.}

\item{ivar}{Character, unit identifier variable name.}

\item{tvar}{Character, time variable name.}

\item{gvar}{Character, cohort variable name.}

\item{observed_att}{Numeric, observed ATT from the caller.}

\item{target}{Character, inference target:
\code{"overall"}, \code{"cohort"}, or \code{"cohort_time"}.}

\item{target_cohort}{Integer or NULL, target cohort
(required when \code{target = "cohort"} or
\code{"cohort_time"}).}

\item{target_period}{Integer or NULL, target period
(required when \code{target = "cohort_time"}).}

\item{rolling}{Character, panel transformation method
(\code{"demean"} or \code{"detrend"}).}

\item{controls}{Character vector or NULL, control variable
names forwarded to estimation functions.}

\item{vce}{Character or NULL, variance estimation method.}

\item{cluster_var}{Character or NULL, clustering variable.}

\item{reps}{Positive integer, number of RI replications
(default 1000).}

\item{seed}{Integer or NULL, random seed for reproducibility.}

\item{method}{Character, \code{"permutation"} (default) or
\code{"bootstrap"}.}
}
\value{
A list with 11 fields:
\describe{
\item{ri_pvalue}{Two-sided RI p-value.}
\item{ri_distribution}{Numeric vector of valid RI stats.}
\item{obs_att}{Observed ATT passed in.}
\item{target}{Inference target used.}
\item{target_cohort}{Target cohort, or NULL.}
\item{target_period}{Target period, or NULL.}
\item{n_valid}{Number of valid replications.}
\item{n_failed}{Number of failed replications.}
\item{failure_rate}{Proportion of failed replications.}
\item{reps}{Number of replications requested.}
\item{method}{Method used.}
}
}
\description{
Performs randomization inference for staggered DiD
by permuting unit-level cohort assignments. Each permutation
re-runs the full pipeline (transform -> estimate -> aggregate)
to build a reference distribution under the sharp null
\eqn{H_0: \tau_i = 0, \forall i} (lw2026 Section 2).
}
\keyword{internal}
