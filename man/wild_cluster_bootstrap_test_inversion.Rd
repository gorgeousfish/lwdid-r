% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wild_bootstrap.R
\name{wild_cluster_bootstrap_test_inversion}
\alias{wild_cluster_bootstrap_test_inversion}
\title{Wild cluster bootstrap test-inversion confidence interval}
\usage{
wild_cluster_bootstrap_test_inversion(
  data,
  y_transformed,
  d,
  cluster_var,
  controls = NULL,
  n_bootstrap = 999L,
  weight_type = "rademacher",
  alpha = 0.05,
  seed = NULL,
  grid_points = 25L,
  ci_tol = 0.01,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{data}{A data frame containing the transformed outcome, treatment
indicator, and cluster identifiers.}

\item{y_transformed}{Character scalar naming the transformed outcome column.}

\item{d}{Character scalar naming the treatment indicator column.}

\item{cluster_var}{Character scalar naming the cluster identifier column.}

\item{controls}{Optional character vector naming time-constant controls.
The current public slice supports these controls on the native bootstrap
path; remaining follow-on work still includes \code{fwildclusterboot} support
for controls-aware WCB.}

\item{n_bootstrap}{Requested number of bootstrap replications.}

\item{weight_type}{Bootstrap weight distribution. Supported values are
\code{"rademacher"}, \code{"mammen"}, and \code{"webb"}.}

\item{alpha}{Significance level used for the bootstrap confidence interval.}

\item{seed}{Optional RNG seed.}

\item{grid_points}{Integer scalar giving the coarse search grid size.}

\item{ci_tol}{Numeric scalar giving the root-finding tolerance.}

\item{parallel}{Logical scalar. When \code{TRUE}, bootstrap replications are
executed in parallel via the \pkg{future} backend. Each replication is
statistically independent (Cameron, Gelbach & Miller 2008), so
parallelization does not affect validity. Uses L'Ecuyer-CMRG RNG streams
for reproducible parallel seeds.}

\item{n_cores}{Integer or NULL. Number of parallel workers. When \code{NULL},
auto-detected as \code{future::availableCores() - 1}.}
}
\value{
An object of class \code{lwdid_wcb_result}.
}
\description{
This public native slice inverts bootstrap hypothesis tests on the native
path. It currently uses the Python-compatible intercept-only restricted
model, while \code{fwildclusterboot} integration remains deferred.
}
