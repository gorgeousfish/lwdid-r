% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lwdid.R
\name{lwdid}
\alias{lwdid}
\title{Lee-Wooldridge Difference-in-Differences Estimation}
\usage{
lwdid(
  data,
  y,
  ivar,
  tvar,
  d = NULL,
  post = NULL,
  gvar = NULL,
  control_group = "not_yet_treated",
  aggregate = "cohort",
  event_time_range = NULL,
  df_strategy = "conservative",
  rolling = "demean",
  estimator = "ra",
  controls = NULL,
  ps_controls = NULL,
  vce = NULL,
  cluster_var = NULL,
  alpha = 0.05,
  ri = FALSE,
  rireps = 1000L,
  seed = NULL,
  ri_method = c("bootstrap", "permutation"),
  season_var = NULL,
  quarter = NULL,
  Q = 4L,
  auto_detect_frequency = FALSE,
  include_pretreatment = FALSE,
  pretreatment_test = TRUE,
  pretreatment_alpha = 0.05,
  exclude_pre_periods = 0L,
  wcb_reps = 999L,
  wcb_type = c("rademacher", "mammen", "webb"),
  wcb_seed = NULL,
  wcb_restricted_model = "with_controls",
  auto_wcb = TRUE,
  use_fwildclusterboot = TRUE,
  trim_threshold = 0.01,
  trim_method = "clip",
  n_neighbors = 1L,
  caliper = NULL,
  caliper_scale = "sd",
  with_replacement = TRUE,
  match_order = "data",
  se_method = NULL,
  boot_reps = 200L,
  return_diagnostics = FALSE,
  parallel = FALSE,
  n_cores = NULL,
  balanced_panel = "warn",
  graph = FALSE,
  gid = NULL,
  graph_options = NULL,
  verbose = "default",
  att_gt = FALSE,
  ...
)
}
\arguments{
\item{data}{data.frame or data.table. Panel data in long format.
Each row represents one unit-period observation.}

\item{y}{character(1). Name of the outcome variable column.}

\item{ivar}{character(1). Name of the unit identifier column.
Required -- no default value.}

\item{tvar}{character(1) or character(2). Name(s) of the time variable
column(s). Length 1 for annual data (e.g., \code{"year"});
length 2 for quarterly data as \code{c(year_var, quarter_var)}.
Required -- no default value.}

\item{d}{character(1) or NULL. Name of the treatment indicator column
(Common Timing mode). Must be time-invariant within each unit.}

\item{post}{character(1) or NULL. Name of the post-treatment indicator
column (Common Timing mode).}

\item{gvar}{character(1) or NULL. Name of the cohort variable column
(Staggered mode). Indicates the first treatment period for each unit.
Never-treated units encoded as NA, 0, or Inf.}

\item{control_group}{character(1). Control group strategy for Staggered
mode. One of \code{"not_yet_treated"}, \code{"never_treated"},
\code{"all_others"}, \code{"auto"}. Default \code{"not_yet_treated"}.}

\item{aggregate}{character(1). Aggregation type for Staggered mode.
One of \code{"none"}, \code{"cohort"}, \code{"overall"}.
Default \code{"cohort"}.}

\item{event_time_range}{Numeric vector of length 2 or NULL. Used when
\code{aggregate = "event_time"} to keep only event times in
\code{c(min_e, max_e)}.}

\item{df_strategy}{Character(1). Degrees-of-freedom aggregation strategy for
event-time summaries. One of \code{"conservative"}, \code{"weighted"}, or
\code{"fallback"}. Default \code{"conservative"}.}

\item{rolling}{character(1). Transformation method. One of
\code{"demean"}, \code{"detrend"}, \code{"demeanq"}, \code{"detrendq"}.
Case-insensitive. Default \code{"demean"}.}

\item{estimator}{character(1). Estimator type. One of \code{"ra"},
\code{"ipw"}, \code{"ipwra"}, \code{"psm"}. Default \code{"ra"}.}

\item{controls}{character vector or NULL. Names of control variable columns.}

\item{ps_controls}{character vector or NULL. Names of propensity score
model control variable columns.}

\item{vce}{character(1) or NULL. Variance-covariance estimator type.
One of \code{NULL}, \code{"hc0"}-\code{"hc4"}, \code{"robust"},
\code{"cluster"}, \code{"bootstrap"}.}

\item{cluster_var}{character(1) or NULL. Name of the cluster variable
column. Required when \code{vce} is \code{"cluster"} or
\code{"bootstrap"}.}

\item{alpha}{numeric(1). Significance level for confidence intervals.
Default 0.05.}

\item{ri}{logical(1). Whether to perform randomization inference.
Default FALSE.}

\item{rireps}{integer(1). Number of RI replications. Default 1000L.}

\item{seed}{integer(1) or NULL. Random seed for reproducibility.}

\item{ri_method}{character(1). RI method. One of \code{"bootstrap"},
\code{"permutation"}. Default \code{"bootstrap"}.}

\item{season_var}{character(1) or NULL. Name of the seasonal indicator
column. Required for \code{demeanq}/\code{detrendq} when \code{tvar}
is length 1.}

\item{quarter}{character(1) or NULL. Backward-compatible alias for
\code{season_var}.}

\item{Q}{integer(1). Number of seasons per cycle. Default 4L.}

\item{auto_detect_frequency}{logical(1). Whether to auto-detect data
frequency. Default FALSE.}

\item{include_pretreatment}{logical(1). Whether to include pre-treatment
period estimates. Default FALSE.}

\item{pretreatment_test}{logical(1). Whether to perform parallel trends
test. Default TRUE.}

\item{pretreatment_alpha}{numeric(1). Significance level for
pre-treatment test. Default 0.05.}

\item{exclude_pre_periods}{integer(1). Number of pre-treatment periods
to exclude from estimation. Default 0L.}

\item{wcb_reps}{integer(1). Number of wild cluster bootstrap replications.
Default 999L.}

\item{wcb_type}{character(1). Wild bootstrap weight type. One of
\code{"rademacher"}, \code{"mammen"}, or \code{"webb"}.}

\item{wcb_seed}{integer(1) or NULL. Optional seed passed to
\code{\link{wild_cluster_bootstrap}}.}

\item{wcb_restricted_model}{character(1). Restricted model passed to
\code{\link{wild_cluster_bootstrap}}. One of
\code{"with_controls"} or \code{"intercept_only"}.}

\item{auto_wcb}{logical(1). When \code{TRUE}, \code{vce="cluster"} with
fewer than 20 clusters automatically upgrades to wild cluster bootstrap.}

\item{use_fwildclusterboot}{logical(1). Whether to request the
\code{fwildclusterboot} adapter before falling back to the native R path.}

\item{trim_threshold}{numeric(1). Propensity score trimming threshold.
Default 0.01.}

\item{trim_method}{character(1). Trimming method. One of \code{"clip"},
\code{"drop"}. Default \code{"clip"}.}

\item{n_neighbors}{integer(1). Number of PSM neighbors. Default 1L.}

\item{caliper}{numeric(1) or NULL. PSM caliper distance.}

\item{caliper_scale}{character(1). Caliper scale. One of \code{"sd"},
\code{"absolute"}. Default \code{"sd"}.}

\item{with_replacement}{logical(1). Whether PSM uses replacement.
Default TRUE.}

\item{match_order}{character(1). PSM match order. One of \code{"data"},
\code{"random"}, \code{"largest"}, \code{"smallest"}.
Default \code{"data"}.}

\item{se_method}{character(1) or NULL. Standard error method.
For IPW/IPWRA: \code{"analytical"} or \code{"bootstrap"}.
For PSM: \code{"abadie_imbens"} or \code{"bootstrap"}.
NULL uses estimator default. Ignored for RA.}

\item{boot_reps}{integer(1). Number of bootstrap replications.
Default 200L.}

\item{return_diagnostics}{logical(1). Whether to return propensity score
diagnostics. Default FALSE.}

\item{parallel}{logical(1). Whether to enable parallel computation for
staggered (g,r) estimation loop. Requires \pkg{future} and
\pkg{future.apply}. Default FALSE.}

\item{n_cores}{integer(1) or NULL. Number of parallel workers. NULL
auto-detects via \code{future::availableCores() - 1}. Ignored when
\code{parallel = FALSE}.}

\item{balanced_panel}{character(1). Panel balance handling. One of
\code{"warn"}, \code{"error"}, \code{"ignore"}. Default \code{"warn"}.}

\item{graph}{logical(1). Whether to produce a plot after estimation.
Default FALSE.}

\item{gid}{NULL or scalar. Unit ID to highlight in the plot.}

\item{graph_options}{list or NULL. Additional options passed to the
plot method.}

\item{verbose}{character(1). Verbosity level. One of \code{"quiet"},
\code{"default"}, \code{"verbose"}. Default \code{"default"}.}

\item{att_gt}{logical(1). R extension parameter. Default FALSE.}

\item{...}{Additional arguments. Currently supports \code{riseed}
(deprecated alias for \code{seed}). Unknown arguments trigger a
warning.}
}
\value{
An S3 object of class \code{\link{lwdid_result}} containing
estimation results, diagnostics, and metadata.
}
\description{
Estimate treatment effects using the Lee-Wooldridge (2025)
Difference-in-Differences framework. Supports both Common Timing
(all units treated at the same time) and Staggered Adoption
(units treated at different times) designs.

The function performs the following steps:
\enumerate{
\item Capture the original call via \code{match.call()}
\item Initialize and reset the global WarningRegistry
\item Process backward-compatible \code{riseed} argument
\item Validate all inputs via \code{\link{validate_inputs}}
\item Dispatch to Common Timing or Staggered estimation
\item Flush warnings and extract diagnostics
\item Populate metadata (call, version)
\item Optionally produce a plot
}
}
\details{
When \code{estimator = "ra"} (default), inference uses the exact
t-distribution (Lee & Wooldridge 2026, Equation 2.10). For
\code{estimator = "ipw"}, \code{"ipwra"}, or \code{"psm"}, inference
uses the asymptotic normal distribution based on:
\itemize{
\item IPW: Lunceford & Davidian (2004) asymptotic normality
\item IPWRA: Cattaneo (2010) asymptotic normality
\item PSM: Abadie & Imbens (2006) asymptotic normality
}
This distinction affects confidence interval construction: RA uses
\code{qt(1 - alpha/2, df)} critical values, while IPW/IPWRA/PSM
use \code{qnorm(1 - alpha/2)}.
}
\examples{
\dontrun{
# Common Timing design
result <- lwdid(data = panel_df, y = "outcome", ivar = "unit_id",
                tvar = "year", d = "treated", post = "post_period")
print(result)
summary(result)

# Staggered Adoption design
result <- lwdid(data = panel_df, y = "outcome", ivar = "unit_id",
                tvar = "year", gvar = "first_treat_year")
print(result)
}

}
\seealso{
\code{\link{validate_inputs}} for input validation,
\code{\link{new_lwdid_result}} for result object structure,
\code{\link{print.lwdid_result}} for printing results,
\code{\link{plot.lwdid_result}} for plotting,
\code{\link{plot_event_study.lwdid_result}} for event study plots
}
