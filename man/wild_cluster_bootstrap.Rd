% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wild_bootstrap.R
\name{wild_cluster_bootstrap}
\alias{wild_cluster_bootstrap}
\title{Wild cluster bootstrap inference for the first public WCB slice}
\usage{
wild_cluster_bootstrap(
  data,
  y_transformed,
  d,
  cluster_var,
  controls = NULL,
  n_bootstrap = 999L,
  weight_type = "rademacher",
  alpha = 0.05,
  seed = NULL,
  impose_null = TRUE,
  full_enumeration = NULL,
  restricted_model = "with_controls",
  use_fwildclusterboot = TRUE
)
}
\arguments{
\item{data}{A data frame containing the transformed outcome, treatment
indicator, and cluster identifiers.}

\item{y_transformed}{Character scalar naming the transformed outcome column.}

\item{d}{Character scalar naming the treatment indicator column.}

\item{cluster_var}{Character scalar naming the cluster identifier column.}

\item{controls}{Optional character vector naming time-constant controls.
The current public slice supports these controls on the native bootstrap
path; remaining follow-on work still includes \code{fwildclusterboot} support
for controls-aware WCB.}

\item{n_bootstrap}{Requested number of bootstrap replications.}

\item{weight_type}{Bootstrap weight distribution. Supported values are
\code{"rademacher"}, \code{"mammen"}, and \code{"webb"}.}

\item{alpha}{Significance level used for the bootstrap confidence interval.}

\item{seed}{Optional RNG seed.}

\item{impose_null}{Logical scalar. Passed through to the native helper.}

\item{full_enumeration}{Optional logical scalar overriding the auto
\code{G <= 12} exact-enumeration boundary for \code{"rademacher"} weights.}

\item{restricted_model}{Accepted for forward compatibility. When
\code{controls = NULL}, the current helper path is the same for
\code{"with_controls"} and \code{"intercept_only"}.}

\item{use_fwildclusterboot}{When \code{TRUE}, emits a fallback warning and uses
the native helper path until \code{fwildclusterboot} integration lands.}
}
\value{
An object of class \code{lwdid_wcb_result}.
}
\description{
The current public slice exposes the already-landed native bootstrap helper
path from \code{clustering_diagnostics.R} as a package-facing function. Remaining
follow-on work is limited to \code{fwildclusterboot} integration and broader
hardening.
}
