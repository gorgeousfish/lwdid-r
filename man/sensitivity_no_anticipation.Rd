% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sensitivity_no_anticipation}
\alias{sensitivity_no_anticipation}
\title{No-Anticipation Sensitivity Analysis}
\usage{
sensitivity_no_anticipation(
  data,
  y,
  ivar,
  tvar,
  gvar = NULL,
  d = NULL,
  post = NULL,
  rolling = "demean",
  estimator = "ra",
  controls = NULL,
  vce = NULL,
  cluster_var = NULL,
  max_anticipation = 3L,
  detection_threshold = 0.1,
  alpha = 0.05,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{data.frame or data.table. Panel data in long format.}

\item{y}{character(1). Outcome variable column name.}

\item{ivar}{character(1). Unit identifier column name.}

\item{tvar}{character(1). Time variable column name.}

\item{gvar}{character(1) or NULL. Cohort variable (staggered).}

\item{d}{character(1) or NULL. Treatment indicator (common timing).}

\item{post}{character(1) or NULL. Post-treatment indicator.}

\item{rolling}{character(1). Transformation method. Default "demean".}

\item{estimator}{character(1). Estimation method. Default "ra".}

\item{controls}{character vector or NULL. Control variables.}

\item{vce}{character(1) or NULL. Variance estimator type.}

\item{cluster_var}{character(1) or NULL. Cluster variable.}

\item{max_anticipation}{integer(1). Maximum exclusion periods. Default 3L.}

\item{detection_threshold}{numeric(1). Detection threshold. Default 0.10.}

\item{alpha}{numeric(1). Significance level. Default 0.05.}

\item{verbose}{logical(1). Print progress. Default TRUE.}

\item{...}{Additional arguments forwarded to underlying \code{lwdid()} calls.}
}
\value{
An S3 object of class \code{lwdid_sensitivity} with type="no_anticipation".
}
\description{
Assess sensitivity of ATT estimates to potential anticipation effects
by systematically excluding pre-treatment periods and detecting
anticipation patterns using dual-method sequential detection.
}
\details{
Paper reference:
\itemize{
\item lw2025 Section 4.4: NA assumption violation handling
\item lw2026 equation 2.14: NA assumption definition
\item lw2026 equation 2.22: truncated pre-period mean
\item lw2025 equation 4.4: Conditional NA assumption (CNAS)
}

Python reference: lwdid-py sensitivity.py sensitivity_no_anticipation()
}
\examples{
\dontrun{
# Common Timing
result <- sensitivity_no_anticipation(
  data = panel_data, y = "outcome", ivar = "unit", tvar = "time",
  d = "treat", post = "post", rolling = "detrend"
)
print(result)
summary(result)
plot(result)
}
}
