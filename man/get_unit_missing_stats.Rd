% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection_diagnostics.R
\name{get_unit_missing_stats}
\alias{get_unit_missing_stats}
\title{Get per-unit observed-Y missing-data statistics}
\usage{
get_unit_missing_stats(
  data,
  y,
  ivar,
  tvar,
  gvar = NULL,
  never_treated_values = c(0, Inf)
)
}
\arguments{
\item{data}{data.frame or data.table in long format.}

\item{y}{Character outcome column.}

\item{ivar}{Character unit identifier column.}

\item{tvar}{Character time column.}

\item{gvar}{Character cohort column or \code{NULL}.}

\item{never_treated_values}{Numeric vector of explicit never-treated markers.}
}
\value{
A data.frame with one row per unit and 17 diagnostic fields.
}
\description{
Get per-unit observed-Y missing-data statistics
}
\details{
The returned fields are computed from observed outcomes rather than row
presence. In particular, \code{n_pre_treatment}, \code{can_use_demean}, and
\code{can_use_detrend} reflect whether a treated unit has enough observed
pre-treatment outcomes to support the corresponding transformation.
}
\examples{
example_panel <- data.frame(
  id = c(1, 1, 2, 2),
  t = c(1, 2, 1, 2),
  g = c(2, 2, 0, 0),
  y = c(NA, 5, 3, 4)
)

get_unit_missing_stats(
  example_panel,
  y = "y",
  ivar = "id",
  tvar = "t",
  gvar = "g"
)
}
