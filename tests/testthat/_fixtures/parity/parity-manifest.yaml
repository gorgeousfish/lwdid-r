version: 1
active_story: story-E8-04
truth_hierarchy:
  - papers-and-math
  - reproducible-numerical-evidence
  - python-reference
  - current-r-behavior
bug_ledger: /Users/cxy/Desktop/lwdid_r/Docs/Python包bug列表.md
parity_layers:
  layer_0:
    name: 论文与 README / example 再现
    status: pending
  layer_1:
    name: 从 Python tests 提炼的单元夹具
    status: passed
  layer_2:
    name: 同数据集 Python -> R comparator
    status: passed
  layer_3:
    name: 真实数据与内置数据集回归
    status: passed
  layer_4:
    name: Monte Carlo
    status: passed
  layer_5:
    name: 发布前回归与 hardening
    status: in-progress
reference_sources:
  papers:
    - /Users/cxy/Desktop/lwdid_r/Docs/lw2025.md
    - /Users/cxy/Desktop/lwdid_r/Docs/lw2026.md
  python: /Users/cxy/Desktop/lwdid_r/lwdid-py_v0.2.3
  stata: /Users/cxy/Desktop/lwdid_r/lwdid_stata
  r_package: /Users/cxy/Desktop/lwdid_r/lwdid-r
key_datasets:
  - smoking
  - castle
  - paper-dgp-common-timing-scenario-1c
  - python-builtins-pending-enumeration
notes:
  - 2026-03-24 21:20 CST correct-course 已新增 `_automation/test-artifacts/parity/20260324-correct-course-e8-04-check-time-tests-bucket-shift.md`。fresh ordinary `devtools::test(filter="staggered-integration|staggered-numerical|conditions")` 当前通过，说明本轮收紧的 staggered stale contracts 与 `lwdid_invalid_param` 条件继承漂移在普通 session 下已转绿；随后 fresh full `devtools::check(--no-manual)` 在 tests 阶段超时前，`00check.log` 已确认 `R code for possible problems` 与全部 `Rd/doc` hardening bucket 当前转绿，但 `tests/testthat.Rout` 仍保存 `_problems/test-clustering-diagnostics-parity-*` 与 `_problems/test-export-*`。因此 `layer_5` 的 canonical blocker 不得再写成 `Rd/doc` bucket 或旧的 three-test ordinary-session list，而应收敛为 installed-package / full-check tests bucket。当前不更新 Python bug ledger。
  - 2026-03-24 21:10 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-04-check-only-staggered-contract-audit.md`。本轮先 fresh rerun `devtools::test(filter="transform-detrend|utils|validate")`，确认 `18:24 CST` 曾列为 live blocker 的 `muffle_detrend` helper、aggregate constant 与 deferred warning-class 三条 ordinary-session contract 当前全部转绿；随后 fresh full `devtools::check(args = c("--no-manual"), error_on = "never")` 仍返回 `1 ERROR / 5 WARNING / 3 NOTE`，但唯一 tests ERROR 已转移为 check-time `test-staggered-integration.R:383/531` 与 `test-staggered-numerical.R:181` 三条 `staggered` 旧 stub / event_time contract。同窗 ordinary `devtools::test(filter="staggered-integration|staggered-numerical")` 当前通过，说明当前 tests 红桶属于 build/check hardening drift，而不是 live theory/Python mismatch。source audit 同时确认：`Docs/lw2025.md` 的 WATT(relative-time) 提供了论文对象真值，但 `aggregate="event_time"` 这个 public API 枚举值属于 R extension，Python public `VALID_AGGREGATE` 与 Stata 公开语法都不把它当成主 aggregate 选项；因此本轮不更新 `Docs/Python包bug列表.md`，controller / hardening owner 下一步应优先修复 `staggered` suite 的过期 stub contract，再 fresh rerun full check。
  - 2026-03-24 21:04 CST qa-parity 已新增 `_automation/test-artifacts/parity/20260324-qa-parity-e8-04-layer5-tests-rebucket.json/.md`。本轮先修 `test-transform-detrend.R`、`test-utils.R` 与 `test-validate.R` 的三条 stale Layer 5 tests contract，并 fresh rerun `devtools::test(filter="transform-detrend|utils|validate")` + `e8_04_layer5_release_regression.R` 当前通过。随后 fresh `devtools::check(args = c("--no-manual"), error_on = "never")` 仍返回 `1 ERROR / 5 WARNING / 3 NOTE`，但 live tests bucket 已从旧的 `transform/utils/validate` 前移为 `test-staggered-integration.R:383/531` 与 `test-staggered-numerical.R:181` 三条 check-time failure。与此同时，`devtools::test(filter="staggered-integration")` 与 `devtools::test(filter="staggered-numerical")` 在普通 session 下当前都通过，因此当前 canonical blocker 应写成 installed-package / full-check 下的 staggered aggregation contract drift，而不是回退成 interactive suite 失败。
  - 2026-03-24 20:49 CST qa-parity 已新增 `_automation/test-artifacts/parity/e8_04_layer5_interactive_bucket_probe.R` 与 `_automation/test-artifacts/parity/20260324-qa-parity-e8-04-layer5-interactive-bucket.json/.md`。fresh rerun `e8_04_layer5_release_regression.R`、`devtools::test(filter="transform-detrend|utils|validate")`、`devtools::test(filter="vce-return-values|vce-integration")`、`devtools::test(filter="visualization-export")` 与 static partial `devtools::check(args = c("--no-manual", "--no-tests", "--no-examples"), document = FALSE, error_on = "never")` 当前均已执行；结论是 interactive Layer 5 test bucket 继续保持转绿，而 static hardening bucket 则稳定停留在 `0 ERROR / 5 WARNING / 3 NOTE` 的 `R code globals` + `Rd/codoc/missing documentation/usage` hygiene。该证据不替代 full `devtools::check(--no-manual)` 的 check-time tests 结论，因此 `layer_5` 继续保持 `in-progress`，`story-E8-04` 仍不得写成 closure-ready。
  - 2026-03-24 18:24 CST qa-parity 已新增 `_automation/test-artifacts/parity/20260324-qa-parity-e8-04-layer5-check-boundary.md/.json`，并修正 `e8_04_layer5_release_regression.R` 的 Markdown 裁决逻辑。fresh rerun `e8_04_layer5_release_regression.R` 当前继续通过，说明 `TC-10.6.18` / `TC-10.6.34` 已退出 live blocker 集；同轮又 fresh rerun `devtools::test(filter="vce-return-values")` 与 `devtools::test(filter="vce-integration")`，把两条 stale VCE test contracts 转绿。随后 fresh `devtools::check(args = c("--no-manual"), error_on = "never")` 返回 `1 error / 5 warnings / 3 notes`；当前 live ERROR 已前移为 `test-transform-detrend.R:378` 的 `muffle_detrend` helper 缺失、`test-utils.R:37` 的 aggregate constants 旧合同，以及 `test-validate.R:1064` 的 deferred aggregate warning-class expectation。Layer 5 因而应保持 `in-progress`，不得继续把 release contracts 或 VCE stale tests 记为 blocker。
  - 2026-03-24 17:26 CST correct-course 已新增 `_automation/test-artifacts/parity/20260324-correct-course-e8-04-hardening-static-convergence.md`。fresh rerun `e8_04_package_hardening_static_regression.R` 当前通过，且 fresh partial `devtools::check(args = c("--no-manual", "--no-tests", "--no-examples"), document = FALSE)` 已确认 `checking code files for non-ASCII characters`、`checking dependencies in R code` 与 `checking for unstated dependencies in 'tests'` 当前都为 `OK`。因此 Layer 5 的 live blocker 已不再包含 non-ASCII / undeclared dependency；剩余 hygiene warning 继续由 later full-check evidence 接管。
  - 2026-03-24 17:31 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-04-release-contract-scope-audit.md`。该证据在当前工作树上 fresh rerun `devtools::check(args = c("--no-manual"))` 后，进一步确认 `aggregate_to_overall.Rd`、`compute_inference.Rd`、`test-sensitivity-pre-period.R` 与 namespace `nobs` 旧 blocker 均已退出 live blocker 集。当前 `AC-17` 的 live ERROR 收敛为 `visualization-export` suite 的两条 release contract：`TC-10.6.18` 的 by-cohort export 中文错误文案在 `session charset: ASCII` 下被转写成 `<U+...>` escaped message，`TC-10.6.34` 的 RI summary 当前只打印 `valid=999` 而非测试要求的 `valid=999/999`。`Docs/lw2025.md` / `Docs/lw2026.md` 不规定这两条字符串合同，因此本轮不构成新的 theory 或 Python drift，bug ledger 保持不变。
  - 2026-03-24 16:40 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-04-ac17-hardening-boundary-audit.md`。该证据在当前工作树上重新执行 source smoke checks 与 fresh `devtools::check(args = c("--no-manual"))`，确认 `story-E8-04` 的 Layer 1-4 parity 结论未被推翻，但 `2026-03-24 15:12 CST` / `16:17 CST` 控制面列出的 `aggregate_to_overall.Rd` parse、namespace `nobs`、`test-sensitivity-pre-period.R` parse 三项 blocker 已部分过期。当前 live `AC-17` 继续收敛为 `compute_inference.Rd` malformed、hidden `.tmp_*`、non-ASCII R 源码、undeclared imports / test dependencies 与 Rd/codoc/documentation hygiene。本轮未发现新的 Python bug，也不改 bug ledger。
  - 2026-03-24 15:33 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-05-selection-diagnostics-source-contract.md`。该文件是 `story-E8-05` 的 sidecar source audit，只为下一顺位 `selection_diagnostics` 实现冻结 theory/source contract，不覆盖当前 `story-E8-04` 的 `AC-17` hardening blocker。source audit 确认：`Docs/lw2025.md:455-457,525` 只固定 selection mechanism 的理论边界；Python `selection_diagnostics.py` 的 MCAR/MAR/MNAR taxonomy、四因素 risk score 与 `pct_usable_detrend < 90` warning 都属于 Python-backed heuristic；`missing-rate / pattern` family 使用 full-grid + `Y`-`NA` 语义，而 `balance / attrition / unit_stats` family 使用 row-presence 语义；`_is_never_treated()` 需把 `NA`、自定义 `never_treated_values` 与任意 `±Inf` 都视为 never-treated。本轮不更新 bug ledger。
  - 2026-03-24 15:12 CST correct-course 已 fresh rerun `clustering-diagnostics|clustering-diagnostics-parity` 与 `devtools::check(args = c("--no-manual"))`，并新增 `_automation/test-artifacts/parity/20260324-correct-course-e8-04-closure-readiness-audit.md`。前者当前继续通过，说明 `story-E8-04` 的 Layer 1-4 parity 已全部收口；但后者当前返回 `1 error / 11 warnings / 7 notes`，因此 `story-E8-04` 仍不能写成 closure-ready，也不得把 active story 切到 `story-E8-05`。当前剩余缺口已转为 `layer_5` / `AC-17` 对应的 package-hardening blocker。
  - 2026-03-24 06:13 CST controller 已确认 `story-E8-03` closure-ready 并切换 active story 到 `story-E8-04`；下一个 parity 目标是待 `clustering_diagnostics.R` 与对应 testthat 入口落地后，再为 E8-04 重建 layer 状态。
  - 2026-03-24 06:58 CST qa-parity 已把 archived Python clustering tests 提炼为 `e8_04_clustering_*_fixture.csv`、`e8_04_clustering_task1_oracle.py` 与 `20260324-qa-parity-e8-04-clustering-task1-python-oracle.json`，并新增 `test-clustering-diagnostics-parity.R`。fresh rerun `devtools::test(filter="clustering-diagnostics")` 当前通过，说明 Layer 1 / Layer 2 已对 `Task E8-04.1` 建立 helper-level executable parity。
  - 同一轮 parity 回归还抓出并修复了 `gvar` 组内 variation 的 `NA` 语义漂移：Python `nunique()` 忽略 `NA`，R 原实现误把 `NA` 计入 unique count；现已在 `lwdid-r/R/clustering_diagnostics.R` 对齐为 `uniqueN(..., na.rm = TRUE)`。因此 E8-04 当前 blocker 不再是“缺文件”，而是 `Task E8-04.2/3` 的 helper / public API 尚未实现。
  - 2026-03-24 07:24 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-04-consistency-na-scope-contract.md`，把 `Task E8-04.2 / 3` 的剩余理论合同再收紧为两点：`.detect_treatment_variation_level()` 与 `check_clustering_consistency()` 都必须复用 pandas `.nunique(dropna=True)` / R `uniqueN(..., na.rm = TRUE)` 语义；而 `check_clustering_consistency()` 的 `treatment_variation_level` 只应对当前 `cluster_var` 做局部判断。现有 `e8_04_clustering_never_treated_fixture.csv` 已可复现：正确语义下 inconsistency share 为 `33.333...%`，误把 `NA` 计为 unique value 则会错放大到 `66.666...%`。本轮未发现新的 Python bug。
  - 2026-03-24 07:50 CST qa-parity 已新增 `_automation/test-artifacts/parity/e8_04_clustering_task23_contract_oracle.py` 与 `20260324-qa-parity-e8-04-clustering-task23-contract.json/.md`，并扩展 `test-clustering-diagnostics-parity.R`。fresh rerun `devtools::test(filter="clustering-diagnostics")` 当前通过；small-cluster diagnosis / public recommender anchors 与 `check_clustering_consistency()` 的 local-scope + `dropna=True` 语义现已具备 executable JSON-backed evidence。E8-04 的 parity blocker 因而进一步收敛为 R public API `recommend_clustering()` / `check_clustering_consistency()` 与 print/S3 输出尚未落地，而不再是 fixture / oracle 缺失。
  - 2026-03-24 08:04 CST correct-course 已把 `recommend_clustering()` / `check_clustering_consistency()`、推荐理由 helper 与 clustering print/S3/NAMESPACE 合同正式落地，并把 public API 断言接入 `test-clustering-diagnostics-parity.R`。fresh rerun `devtools::test(filter="clustering-diagnostics")` 当前通过，因此 E8-04 的 parity blocker 已从 public API 缺口，转移为 Task `4/5` 与 Layer 3 / 4 的下一层 parity 收口。
  - 2026-03-24 08:26 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-04-layer34-source-contract.md`，把 `story-E8-04` 下一层 parity 的合法来源固定为两层：论文 `Docs/lw2026.md:641-643` 只提供“处理在更高层级分配时可在该层级聚类”的 cross-sectional regression 真值，而 Python archived `test_clustering_simulated.py` / `test_clustering_empirical.py` / `test_clustering_monte_carlo.py` 则提供 Layer 3 state-policy / few-cluster / hierarchical panel 场景与 Layer 4 coverage / size / power acceptance bands。后续 clustering Monte Carlo parity 不应写成 exact-number oracle，本轮也未发现新的 Python bug。
  - 2026-03-24 08:47 CST story-worker 已新增 `_automation/test-artifacts/parity/e8_04_clustering_layer3_state_policy_oracle.py`、`20260324-story-worker-e8-04-layer3-state-policy.json/.md` 并扩展 `test-clustering-diagnostics-parity.R`。fresh rerun `devtools::test(filter="clustering-diagnostics")` 当前通过；E8-04 已具备首个 Layer 3 state-policy regression，直接把 archived Python `TestStateLevelPolicy` 场景冻结为 executable oracle。当前 parity blocker 因而进一步收敛为 few-cluster / hierarchical supplemental Layer 3 与 Layer 4 Monte Carlo，而不再是“Layer 3 未启动”。
  - 2026-03-24 08:51 CST qa-parity 已新增 `_automation/test-artifacts/parity/20260324-qa-parity-e8-04-layer3-state-policy.md`，并 fresh rerun `devtools::test(filter="clustering-diagnostics-parity")` 复核 `20260324-story-worker-e8-04-layer3-state-policy.json` 当前仍可被 R 侧 public workflow 直接消费。本轮未发现新的 Python bug；Layer 3 blocker 继续保持为 few-cluster / hierarchical / built-in empirical 场景，而不是回退为“state-policy 未验证”。
  - 2026-03-24 09:36 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-04-task45-boundary-contract.md`，把 E8-04 当前剩余的 Task `4/5` 合同固定为 score arithmetic / BalanceScore identity / strict `<20` WCB / strict `<5` consistency / alternatives<=2 / internal-vs-public sorting boundary，并额外记录 `G=0` 当前只属于 undefined empty-data boundary：Python helper 会报 `int(NaN)`，R helper 则返回零聚类结构，故本轮不新增 Python bug ledger 条目。
  - 2026-03-24 10:07 CST qa-parity 已新增 `_automation/test-artifacts/parity/e8_04_clustering_layer3_few_cluster_oracle.py`、`e8_04_clustering_layer3_few_cluster_fixture.csv`、`20260324-qa-parity-e8-04-layer3-few-cluster.json/.md` 并扩展 `test-clustering-diagnostics-parity.R`。fresh rerun `devtools::test(filter="clustering-diagnostics-parity|clustering-diagnostics")` 当前通过；该轮把 archived Python `TestFewClusters` 冻结为第二个 Layer 3 public-workflow oracle，同时收口了 `diagnose_clustering()` 在 `<20 clusters` 路径上的 warning string drift。当前 parity blocker 因而从 “few-cluster / hierarchical / built-in empirical Layer 3” 进一步收敛为 “`Task 5.3` 仍缺 hierarchical / built-in empirical / common-timing supplemental Layer 3 + Layer 4 Monte Carlo”，本轮未发现新的 Python bug。
  - 2026-03-24 12:19 CST correct-course 已新增 `_automation/test-artifacts/parity/20260324-correct-course-e8-04-boundary-state-convergence.md`，并 fresh rerun `devtools::test(filter="clustering-diagnostics|clustering-diagnostics-parity")` 当前通过；`test-clustering-diagnostics.R` 现已把 `Task 4.4/4.8/5.2` 冻结为 executable boundary regression，因此 E8-04 的 parity blocker 现只剩 `Task 5.3` 的 hierarchical / built-in empirical / common-timing supplemental Layer 3，以及 Layer 4 Monte Carlo acceptance-band parity。本轮未发现新的 Python bug。
  - 2026-03-24 12:33 CST qa-parity 已新增 `_automation/test-artifacts/parity/e8_04_clustering_layer3_hierarchical_oracle.py`、`e8_04_clustering_layer3_hierarchical_fixture.csv`、`20260324-qa-parity-e8-04-layer3-hierarchical.json/.md` 并扩展 `test-clustering-diagnostics-parity.R`。fresh rerun `devtools::test(filter="clustering-diagnostics|clustering-diagnostics-parity")` 当前通过；该轮把 archived Python `test_clustering_empirical.py` 的 hierarchical panel public workflow 冻结为第三个 executable Layer 3 oracle，因此当前 parity blocker 进一步收敛为 `Task 5.3` 的 built-in empirical / common-timing supplemental Layer 3，以及 Layer 4 Monte Carlo。本轮未发现新的 Python bug。
  - 2026-03-24 12:42 CST story-worker 已新增 `_automation/test-artifacts/parity/e8_04_clustering_layer3_smoking_common_timing_oracle.py` 与 `20260324-story-worker-e8-04-layer3-smoking-common-timing.json/.md`，并扩展 `test-clustering-diagnostics-parity.R`。fresh rerun `devtools::test(filter="clustering-diagnostics-parity|clustering-diagnostics")` 当前通过；该轮把 `smoking` common-timing built-in empirical `state` public workflow 冻结为第四个 executable Layer 3 oracle，因此 `Task 5.3` 已全部收口，E8-04 的 parity blocker 现只剩 Layer 4 Monte Carlo acceptance-band parity。本轮未发现新的 Python bug。
  - 2026-03-24 13:22 CST theory-parity 已新增 `_automation/test-artifacts/parity/e8_04_clustering_layer4_monte_carlo_contract.yaml` 与 `_automation/test-artifacts/parity/20260324-theory-parity-e8-04-layer4-monte-carlo-contract.md`。该证据把 `test_clustering_monte_carlo.py` 的 Layer 4 contract 压缩为 9 个 machine-readable scenarios：`G=50/20/10` standard coverage、`G=10` wild-bootstrap coverage 与 relative-gap、`G=30` size / power，以及 unbalanced-treatment / heterogeneous-cluster-size robustness。source audit 同时确认 `Docs/lw2026.md:641-643` 只提供 higher-level clustering 的论文真值，这些数值阈值都属于 Python-backed QA target，而不是 paper theorem；因此当前 direct blocker 仍是“把上述 bands 落成 R 侧 executable regression”，而不是重新界定理论边界。本轮未发现新的 Python bug。
  - 2026-03-24 14:00 CST correct-course 已 fresh rerun `devtools::test(filter="clustering-diagnostics")` 与 `devtools::test(filter="clustering-diagnostics-parity")`，并复核 `_automation/test-artifacts/parity/20260324-story-worker-e8-04-layer4-coverage-large-g.json/.md` 与 `20260324-qa-parity-e8-04-layer4-standard-scenarios.json/.md`。当前 Layer 4 的 standard cluster-robust 7 个 scenario 已全部具备 executable evidence，因此 `layer_4` 状态应从 `pending` 收敛为 `in-progress`，而剩余 parity blocker 只保留 `wild_bootstrap_coverage_small_g` 与 `wild_bootstrap_relative_gap`。本轮未发现新的 Python bug。
  - 2026-03-24 14:23 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-04-layer4-wild-bootstrap-source-contract.md`，并补充 `e8_04_clustering_layer4_monte_carlo_contract.yaml` 的 wild-bootstrap 细节。source audit 确认 archived Python tests 虽请求 `n_bootstrap=199`，但在 `G=10` 且默认 `weight_type='rademacher'` 时，`wild_cluster_bootstrap()` 会自动启用 full enumeration，实际枚举 `2^10=1024` 个权重组合；同时默认 `impose_null=TRUE`、CI 使用 `percentile_t`、p-value 采用 `|t*| >= |t_orig|`。因此 E8-04 的最终 direct blocker 不是泛指“199 次随机 bootstrap”，而是复现上述 Python default WCB contract。本轮未发现新的 Python bug。
  - 2026-03-24 14:53 CST qa-parity 已新增 `_automation/test-artifacts/parity/20260324-qa-parity-e8-04-layer4-wild-bootstrap-scenarios.json/.md`，并 fresh rerun `devtools::test(filter="clustering-diagnostics-parity")` 当前通过。该证据确认 `wild_bootstrap_coverage_small_g` 的 `coverage_rate = 0.96`、`wild_bootstrap_relative_gap` 的 `coverage_rate_gap = 0.05`，且两条场景都保持 `requested_n_bootstrap = 199`、`actual_n_bootstrap = 1024`、`weight_type = rademacher`、`impose_null = TRUE`、`ci_method = percentile_t` 与 `full_enumeration = TRUE`。因此 `story-E8-04` 的 parity blocker 已清空，当前只待 controller 依据 fresh rerun 决定 active story 切换到 `story-E8-05`。本轮未发现新的 Python bug。
  - 2026-03-24 14:51 CST story-worker 已新增 `_automation/test-artifacts/parity/20260324-story-worker-e8-04-layer4-wild-bootstrap.json/.md`，并在 `lwdid-r/tests/testthat/test-clustering-diagnostics-parity.R` / `lwdid-r/R/clustering_diagnostics.R` 先 RED 后 GREEN 落地 `wild_bootstrap_coverage_small_g` 与 `wild_bootstrap_relative_gap` 的 executable regression。fresh rerun `devtools::test(filter="clustering-diagnostics-parity")` 与 `devtools::test(filter="clustering-diagnostics")` 当前通过；R 侧现得到 `coverage_rate = 0.96`、`coverage_rate_gap = 0.05`，且两条 scenario 都已锁定 `requested_n_bootstrap = 199` / `actual_n_bootstrap = 1024`、`impose_null = TRUE`、`ci_method = percentile_t`。因此 `layer_4` 现已从 `in-progress` 收敛为 `passed`，E8-04 的 parity blocker 当前已清零。本轮未发现新的 Python bug。
  - parity 证据必须优先复用相同数据集。
  - 若发现 Python 与论文冲突，先登记 bug，再决定 R 是否跟随。
  - 2026-03-23 qa-parity 已落地并执行 story-E8-03 shared-data comparator；Layer 2 synthetic common-timing fixture 当前通过。
  - 2026-03-23 theory-parity 已确认 Python `sensitivity_analysis()` 在 near-zero `demean_att` 时会对 `overall_assessment` 漏掉 transformation issue 的零基线 guard；此类场景不得把 Python exact string summary 当真值，需交叉检查 bug ledger 与字段级证据。
  - 2026-03-23 qa-parity 已新增并执行 smoking 内置数据 shared-data comparator；Layer 3 common-timing / no-controls 路径当前 exact / numeric 通过。
  - 2026-03-23 qa-parity 已把 smoking comparator 接入 `testthat` 回归，避免 real-data parity 证据停留在一次性 JSON。
  - 2026-03-23 qa-parity 已新增并执行 castle 内置数据 staggered shared-data comparator，并接入 `testthat` 回归；Layer 3 现已同时覆盖 smoking/common-timing 与 castle/staggered 两条真实数据主路径。
  - 2026-03-23 theory-parity 已完成 plot oracle source audit：`REQ-29` 与 Python pre-period plot 均固定使用 `±25%` baseline band，`REQ-31` 与 Python no-anticipation plot 均只要求红色空心圆；`TC-10.2.23/27` 当前不应被视为 source-backed parity blocker。
  - 2026-03-23 20:00 CST correct-course 已 rerun `plot-diagnostics|sensitivity-comprehensive`，相关 suites 当前通过；Layer 3 证据不再受 plot regression 阻塞，剩余 parity 主缺口收敛为 Layer 4 Monte Carlo 与 controls 分支 comparator 审计。
  - 2026-03-23 qa-parity 已新增 `e8_03_monte_carlo_common_timing_comparator.py`、fixture `e8_03_monte_carlo_common_timing_fixture.csv` 与 JSON oracle `20260323-qa-parity-e8-03-monte-carlo-common-timing.json`。
  - 2026-03-23 21:01 CST correct-course 已 rerun `sensitivity-comprehensive-monte-carlo-parity`；Layer 4 common-timing Monte Carlo 当前通过，剩余 parity 主缺口收敛为 controls 分支 comparator 审计。
  - 2026-03-23 theory-parity 已确认 `castle` 的合法 unit-level controls (`income`, `unemployrt`, `poverty`) 场景下，Python 会在 `N_1 <= K+1` 时跳过 paper-valid `eq. 2.18` simple-controls regression；该 drift 已记入 bug ledger，后续 controls comparator 不得把 Python no-controls fallback 当真值。
  - 2026-03-23 theory-parity 同时确认 `smoking` 常见 raw controls (`lnincome`, `beer`, `age15to24`, `lretprice`) 属于 time-varying `X_it`；若要推进 common-timing controls parity，必须先冻结为 shared unit-level `X_i` fixture。
  - 2026-03-23 theory-parity 已新增 `smoking` controls fixture contract 审计：论文 `(2.17)-(2.18)`、Stata centered-`X` 路径，以及 Python/R 公共 validation 都要求 time-constant `X_i`；因此 raw `smoking` controls 只能作为 blocker evidence，后续 comparator 必须显式声明 frozen `X_i` 生成规则，不能暗用 R 内部 pre-period proxy。
  - 2026-03-23 theory-parity fresh audit 已确认 `castle` controls comparator 已由 qa-parity 落地为 bug-aware oracle `_automation/test-artifacts/parity/20260323-qa-parity-e8-03-castle-controls-comparator.json`，且 `devtools::test(filter="sensitivity-comprehensive-staggered-controls-parity")` 当前通过；因此 controls parity blocker 不再是泛指 “controls comparator 审计”，而是收窄为 `smoking` common-timing shared frozen `X_i` fixture / comparator。
  - 2026-03-23 qa-parity 已新增 `smoking` common-timing shared frozen `X_i` fixture、bug-aware comparator 与说明文档；2026-03-23 theory-parity fresh rerun `sensitivity-comprehensive-common-controls-parity` 当前通过，controls comparator 已不再是缺文件 blocker。
  - 2026-03-23 theory-parity 已确认 `smoking` common-timing frozen-`X_i` comparator 与 `castle` staggered controls comparator 共同落在 `PY-EST-002` 作用域：Python 在合法 small-sample controls 下仍走 `bugged-no-controls-fallback`，后续 exact / numeric parity 需以 bug ledger waiver 为准。
  - 2026-03-24 00:28 CST theory-parity fresh rerun：`devtools::test(filter="sensitivity-comprehensive-common-controls-parity")` 继续通过；结合 `_automation/test-artifacts/parity/20260324-theory-parity-e8-03-controls-state-convergence.md`，确认 controls comparator 持续保持 executable evidence，active story 的剩余缺口不再包含 Task 6.3-6.5。
  - 2026-03-24 01:05 CST qa-parity 已新增 `smoking` common-timing Stata-backed precision comparator `_automation/test-artifacts/parity/e8_03_smoking_stata_precision_comparator.py`，并生成 JSON oracle / 中文证据；`devtools::test(filter="stata-precision")` 当前通过，说明 comprehensive transformation path 的 `demean/detrend` ATT/SE 已有可执行的 Stata 精度锚点，但 Layer 5 仍未完成 FATAL 继承与全字段数值合理性总检。
  - 2026-03-24 02:10 CST correct-course fresh rerun：`devtools::test(filter="sensitivity-comprehensive-hardening-parity")` 当前通过；`_automation/test-artifacts/parity/20260324-qa-parity-e8-03-comprehensive-numeric-audit.json` 与同名 `.md` 已按当前 comprehensive 输出重生成，确认 `smoking`、`castle` 与 `smoking-frozen-controls` 三条 `type="all"` real-data 路径当前无非有限数值，且 ATT-like 标量均落在 `|ATT| < 10 * sd(y)` hardening 边界内。Task `8.6` 不再是当前 blocker，但 Layer 5 仍待 `8.5` 的 FATAL inheritance 回归补齐。
  - 2026-03-24 02:30 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-03-fatal-inheritance-source-audit.md`；source audit 确认 `lwdid_sensitivity*` 与 comprehensive `.run_*_comparison()` 会把 `control_group` / `aggregate` / `ri` 等参数继续透传给 `lwdid()`，因此 FATAL-001/002/004 的真值边界已锁定，`ri=TRUE` 时 FATAL-003 的架构继承也已明确。Layer 5 当前剩余的是 executable regression gap，而不是理论来源不清。
  - 2026-03-24 02:57 CST qa-parity 已新增 `_automation/test-artifacts/parity/e8_03_fatal_inheritance_audit.R`、对应 JSON oracle / 中文说明，以及回归测试 `lwdid-r/tests/testthat/test-sensitivity-comprehensive-fatal-inheritance-parity.R`；fresh rerun `devtools::test(filter="fatal-inheritance-parity")` 当前通过。该证据确认 `FATAL-001` 的 strict `NYT` baseline 与 `FATAL-002/004` 的 aggregation auto-switch + NT weighted-average 当前都已具备 executable regression，Layer 5 剩余缺口收窄为 `FATAL-003` 的 story-level `ri=TRUE` 覆盖。
  - 2026-03-24 03:26 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-03-ri-pvalue-formula-drift.md`；source audit 确认 `Docs/lw2025.md` / `Docs/lw2026.md`、Python 与 Stata 的 RI p-value 定义均为 `c/N`，而 `lwdid-r/R/randomization.R` 当前 common / staggered 两条 RI 都使用 continuity correction `(1 + c) / (N + 1)`。该 drift 不推翻 FATAL-003 的架构继承，但说明 Layer 5 后续若要声称 exact RI parity，不能把当前 R `ri_pvalue` 当作 source-backed oracle。
  - 2026-03-24 03:52 CST qa-parity 已新增 `smoking` comprehensive `pre_period` / `no_anticipation` Stata comparator `_automation/test-artifacts/parity/e8_03_smoking_stata_sensitivity_branches_comparator.py`，并生成 `_automation/test-artifacts/parity/20260324-qa-parity-e8-03-smoking-stata-sensitivity-branches.json` 与同名 `.md`。fresh rerun `devtools::test(filter="sensitivity-comprehensive-stata-precision-parity")` 当前通过；证据确认 `pre_period n_pre=2:19` 与 `exclude=0:3` 的 ATT/SE 已具备 Stata-backed oracle，而 `n_pre=1` 在 Stata 端返回 `r(2001)`，故 Task `8.4` 已从“缺少 pre_period / no_anticipation 锚点”收窄为“仅剩 `pre_period n_pre=1` 的已知 Stata 边界”。
  - 2026-03-24 03:45 CST story-worker 已在 `test-sensitivity-comprehensive-fatal-inheritance-parity.R` 新增 common / staggered `type="transformation"` + controls + `ri=TRUE` 的 story-level regression，并把 `lwdid-r/R/randomization.R` 的 common / staggered RI p-value 公式从 continuity correction 对齐到论文 / Python / Stata 一致的 `c/N`。
  - 2026-03-24 03:58 CST correct-course fresh rerun：`devtools::test(filter="randomization|sensitivity-comprehensive|sensitivity-comprehensive-fatal-inheritance-parity|sensitivity-comprehensive-stata-precision-parity")` 当前通过；Layer 5 不再把 `8.5` 视为 blocker，剩余 gap 收敛为 `8.4` 的 `pre_period n_pre=1` Stata 边界，以及后续 `E8-04` / `E8-05` 缺文件。
  - 2026-03-24 04:26 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-03-pre-period-npre1-stata-boundary.md`；source audit 确认论文 `(2.21)`、R 与 Python 都允许 common-timing `demean` 的 `n_pre=1`，而 `smoking` 上的 `r(2001)` 来自 `lwdid_stata/lwdid.ado` 对单个 pre-period 观测执行 unit-wise `regress` 的实现边界。后续 Task `8.4` 不应再把该分支写成 R/Python theory drift，而应写成 Stata boundary 的 waiver / manual oracle 决策。
  - 2026-03-24 05:32 CST theory-parity 已新增 `_automation/test-artifacts/parity/20260324-theory-parity-e8-03-pre-period-npre1-manual-oracle.md` 与同名 `.json`；该证据按论文 `(2.21)+(2.20)+(2.10)` 直接重建 `smoking` `pre_period n_pre=1` 的 manual oracle，得到 `ATT = -0.306762820273115`、`SE = 0.084015006052754`，与当前 R 完全一致、与 Python 仅差浮点噪声。Task `8.4` 因而从“waiver / manual oracle 决策”进一步收敛为“是否把该 JSON 接入回归测试，或保留显式 waiver”的 closure 选择。
  - 2026-03-24 06:08 CST qa-parity 已把 `_automation/test-artifacts/parity/20260324-qa-parity-e8-03-smoking-stata-sensitivity-branches.json/.md` 更新为 manual-oracle-backed QA contract，并在 `lwdid-r/tests/testthat/test-sensitivity-comprehensive-stata-precision-parity.R` 新增 `n_pre=1` 的 ATT/SE/p/CI regression。fresh rerun `devtools::test(filter="sensitivity-comprehensive-stata-precision-parity")` 当前通过；Task `8.4` 不再是 Layer 5 blocker，Layer 5 现已具备完整 executable evidence。
